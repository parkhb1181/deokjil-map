node_modules/
dist/
.env
.env.local
*.local

# 수집 원본 (저작물 원문, 재게시하지 않는다. 가공 결과만 커밋)
data/raw/*
!data/raw/README.md

.DS_Store
Thumbs.db
.vscode/
.idea/

.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo
*.tsbuildinfo

# 크롤러 점검용으로 루트에 떨어질 수 있는 임시 파일 (사이트맵·페이지 덤프)
# 원본은 커밋하지 않는다. data/raw/ 규칙과 같은 이유
*.sitemap.xml
sitemap*.xml
om*.xml
*_detail.html
chunks.txt

# 화이트리스트 누락 후보 리포트, 점검용 산출물이라 커밋하지 않는다
data/miss-candidates.json
.bkit/

# OG 카드용 축소 사진. 빌드마다 다시 만든다
.og-cache/

# IndexNow 키. 도메인 소유 증명이라 public/ 의 키 파일과 짝이다
.indexnow-key
