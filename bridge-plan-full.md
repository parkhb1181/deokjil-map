# 덕질 현장 실시간 지도, 제품 기획

> **한 줄 정의**
> 팝업·생카 현장의 대기와 굿즈 재고를 실시간으로 보여주는 위치 기반 지도.
> 그 앞에 모인 사람들로 포카 현장 교환을 켠다.

작성일 2026-08-27 · 내부 기획 문서

> **문서 관계**
> 본 문서는 브릿지 제품의 **전체 구상**이다. 실제 착수는 이 중 조회 기능만 떼어낸
> 수요 검증 PoC로 시작한다. [poc-plan.md](poc-plan.md) 참조.
> PoC 범위는 조회(F1) + 제보(F2·F3)이며, GPS 게이트(F4)는 판정만 기록하고 게이트는 해제한다.
> 제외 항목(F5 제보 등록 · F6 교환 · 밀도 지표 · 안전 설계)은 폐기가 아니라 **이월**이며,
> PoC 통과 시 9번 M4 단계부터 본 문서로 복귀한다.

---

## 1. 배경과 목적

본 서비스는 **반경 기반 포토카드 현장 교환**이다. 실시간으로 주변 사용자의 포카를 보고,
채팅하고, 만나서 교환한다. 문제는 콜드스타트다. 반경 안에 사람이 없으면 제품이 존재하지 않는다.

전국에 흩어진 사용자 5만 명은 반경 2km 안에 0명이다.
따라서 선행 제품(브릿지)의 목표는 **사용자 수가 아니라 특정 좌표의 사용자 밀도**다.

### 왜 이 브릿지인가

| 후보 | 기각 사유 |
| --- | --- |
| 생카 정보 앱 | 정보 조회는 집에서 하는 행동. 지리적 밀도를 만들지 못함. 오프메이트 존재 |
| 포카 도감 · 시세 | 포카마켓 · 포카판이 이미 점유. 정면 충돌 |
| **현장 실시간 지도** | **현장에 있어야만 생성되는 데이터. 사용 = 밀도 생성** |

기존 서비스는 전부 **아티스트 축**으로 묶는다(최애 이름 → 일정).
**위치 축**으로 묶는 서비스가 없고, 그 축이 곧 본 서비스의 매칭 축이다.

---

## 2. 목표 / 비목표

### 목표 (MVP)

- 홍대 · 합정 · 성수 3구역에서 열리는 생카 · 팝업을 **위치 기준**으로 조회
- 각 현장의 **굿즈 재고**와 **대기 시간**을 사용자 제보로 실시간 표시
- 제보 시 GPS 검증을 통해 **반경 내 활성 사용자 위치 데이터**를 축적

### 비목표 (이번에 하지 않음)

- 전국 커버리지, 3구역 밖은 다루지 않는다
- 아티스트 화이트리스트, 지역 안의 이벤트는 아이돌이든 아니든 전부 담는다
- 포카 교환 · 채팅, **밀도 임계 도달 전까지 UI에 노출하지 않는다**
- 생카 대관 중개, 덕플레이스 영역
- 네이티브 앱, 브릿지는 웹(PWA)으로 검증한다

---

## 3. 사용자와 핵심 시나리오

주 사용자는 수도권 거주 10~20대 K-pop 팬. 주말에 한 구역에서 여러 곳을 도는
"도장깨기" 투어가 이미 문화로 존재한다.

### S1. 가기 전, 동선 짜기
> 토요일에 홍대 갈 건데 뭐뭐 열려 있지?
> → 지도에서 홍대 반경 1km · 오늘 날짜 → 생카 14곳, 팝업 2곳 → 순서대로 담기

### S2. 현장에서, 헛걸음 방지
> 지금 가면 아크릴 스탠드 살 수 있나?
> → 팝업 상세 → "아크릴 스탠드 품절(8분 전) / 포토카드 세트 재고 있음(20분 전)"

### S3. 줄 서는 중, 체류
> 대기 40분. 할 게 없다.
> → 랜덤 뽑기 현황 확인 → 내가 뽑은 것 등록 → **"근처에 이 멤버 원하는 사람 3명"**

S3가 본 서비스로 넘어가는 지점이다. 랜덤 포카를 뽑은 직후가 교환 욕구의 최고조다.

---

## 4. 기능 범위

우선순위는 **재고 → 혼잡도** 순이다.
혼잡도는 가기 전에 한 번 보고 끝이지만, 재고는 줄 서는 동안 계속 본다.
체류 시간이 길수록 교환 전환 확률이 오른다.

### F1. 위치 기반 이벤트 조회 `필수`

- 지도 + 리스트 토글. 기본은 현재 위치 반경 1km, 오늘 날짜
- 구역 프리셋(홍대 · 합정 / 성수 / 전체)
- 이벤트 카드: 대상, 유형, 카페명, 운영시간, 특전, 남은 일수
- 원문 링크 필수 노출 (출처 표기)

### F2. 굿즈 재고 현황 `필수`

- 팝업별 굿즈 마스터를 사전 등록 (공식 MD 라인업 공지 기반, 팝업당 10~30품목)
- 품목별 3-state 제보: `재고 있음` / `품절` / `수량 제한`
- 각 줄에 마지막 갱신 시각 병기. 품절 항목은 취소선
- **랜덤 품목은 별도 처리**, "품절" 개념이 아니라 "지금 뭐가 나오나"가 관심사

### F3. 실시간 혼잡도 / 웨이팅 `필수`

- 한 탭 제보: `대기 없음` / `10분 이내` / `30분` / `1시간+`
- 텍스트 입력을 요구하지 않는다. 요구하는 순간 아무도 안 쓴다
- 지도 핀 색으로 혼잡도 표현
- **30분 경과 제보는 신뢰도 감쇠**, 오래된 정보는 없는 정보보다 나쁘다

### F4. GPS 검증 제보 `필수`

- 장소 반경 200m 안에서만 제보 버튼 활성화
- 허위 제보 방어 + 활성 사용자 위치 수집을 동시에 달성

### F5. 제보 등록 (커버리지 보완) `선택`

- 사용자가 트윗 링크 붙여넣기 또는 안내 이미지 업로드
- 이미지 → 구조화 데이터 파싱 (Claude API)
- 신뢰도 `user_submitted`로 저장, 검수 후 승격

### F6. 포카 교환 `잠금`

밀도 임계 도달 전까지 코드에는 있되 UI에 노출하지 않는다.
실패한 첫 매칭 경험은 거의 복구되지 않는다.

---

## 5. 데이터 수집 전략

### 5.1 생카, 주최자가 아니라 **카페**에서 받는다

생카 정보의 원본은 주최자가 X에 올리는 안내 이미지 한 장이다.
문제는 노드가 너무 많고(이벤트마다 새 계정), 정보가 텍스트가 아니라는 것.

**주최자는 매번 바뀌지만 카페는 고정 사업자다.**
홍대에서 하루 100곳이 돌아간다는 건 그 100곳이 반복 대관을 받는다는 뜻이다.
카페 200곳의 대관 캘린더를 확보하면 이벤트 수천 건이 따라온다.

| 순위 | 소스 | 노드 수 | 인센티브 | 단계 |
| --- | --- | --- | --- | --- |
| 1 | **생카 대관 카페** | 수백 곳 | 공석 채우기 | **시작점** |
| 2 | 직접 발품 | 첫 주말 30~50곳 |, | 시드 |
| 3 | 사용자 제보 | 무한 | 자기가 갈 곳 | 스케일 |
| 4 | 안내 이미지 파싱 | 무한 |, | 보완 |
| 5 | 주최자 직접 등록 | 이벤트당 1 | 초기엔 없음 | 후순위 |

### 5.2 팝업, 공식 소스가 있다

- 기획사 공식 채널 (하이브 · 위버스 · SM · JYP · YG · 카카오엔터 · 스타쉽)
- 유통 채널 보도자료 (더현대 서울, 코엑스, 성수 에스팩토리 · LCDC)
- **장소가 사실상 고정**, 장소 마스터를 먼저 만들면 이벤트만 갈아 끼우면 된다
- 월 10~20건 수준이라 초기엔 수기 입력으로 충분

### 5.3 X API는 쓰지 않는다

Free 티어는 읽기가 사실상 막혀 있고, 검색은 Basic(월 200달러)부터다.
위 1~3번으로 대체 가능하므로 **로드맵에서 제외**한다.
필요 시 `publish.twitter.com/oembed`(무인증 · 무료)로 링크 단위 조회만 사용.

### 5.4 법적 가드레일

- 타 서비스 리스팅 무단 크롤링 금지, 법적 · 관계적 리스크
- **안내 이미지 자체를 복제 게시하지 않는다.** 사실 정보(장소 · 기간 · 특전)만
  재구성하고 원문 링크를 붙인다. 일정은 저작물이 아니지만 포스터는 저작물이다
- 사용자 업로드 이미지는 사용자 제공 데이터로 처리 (약관 명시)

---

## 6. 커버 범위

### 지역

1차: **홍대 · 합정** (반경 1km)
2차: **성수**
3차: 상암 · 여의도 (음방 공방, 주 5회 고정 발생, 정보 빈틈 최대)

### 아티스트, 화이트리스트를 두지 않는다

위치축 제품에 아티스트 화이트리스트는 모순이다.
"오늘 홍대에 뭐 열려?"에 "등록된 20팀만 보여드립니다"라고 답하면 제품이 무너진다.
**아티스트 목록은 검색 · 필터용으로만 존재한다.**

단, **수집 노력의 우선순위**는 둔다. 핵심 원리는 하나다.

> **생카 발생 빈도 ≈ 멤버 수.** 단위는 그룹이 아니라 **멤버**다.

| 티어 | 대상 | 근거 |
| --- | --- | --- |
| T1 | NCT 계열(20+), 세븐틴(13), 트레저(10), 제로베이스원(9), 스트레이키즈(8), BTS(7), 엔하이픈(7), 보이넥스트도어(6), 라이즈(6) | 9팀 = 멤버 86명. 연 생일 이벤트 수백 건 |
| T2 | TXT, 엑소, 투어스, 데이식스, 몬스타엑스, 크래비티, 앤팀, 넥스지, 킥플립 | 국내 오프 활동 강함 |
| T3 | 아이브, 있지, 엔믹스, 르세라핌, 트와이스, 에스파, 아일릿, 베이비몬스터 | 개인 팬덤 강함 |
| T4 | 버추얼(플레이브 등), 애니 · 게임 캐릭터, 배우 | 생카 밀도 의외로 높음. 스키마만 열어두고 UGC로 수용 |

T1~T3 = 약 25팀, 멤버 180~200명. MVP 커버리지로 충분하다.

---

## 7. 데이터 모델

위치축 제품이므로 **장소가 1급 엔티티**다.
이벤트를 먼저 만들고 장소를 속성으로 넣으면 "이 카페에서 열렸던 생카" 같은 걸 못 만든다.

```
Place (장소 마스터)          카페 · 팝업 공간. 좌표, 구역
 └ Event (이벤트)            대상, 유형, 기간, 특전, 원문링크, 신뢰도
     ├ Goods (굿즈)          팝업 한정. 품목 마스터
     │   └ StockReport       품목별 재고 제보 (UGC)
     └ CrowdReport           대기 제보 (UGC)
```

```sql
create table place (
  id            uuid primary key,
  name          text not null,
  address       text not null,
  geom          geography(point, 4326) not null,   -- PostGIS
  district      text not null,                     -- 'hongdae' | 'seongsu' | ...
  kind          text not null,                     -- 'cafe' | 'popup_venue'
  created_at    timestamptz default now()
);
create index on place using gist (geom);

create table event (
  id            uuid primary key,
  place_id      uuid references place(id),
  subject       text not null,      -- 아이돌 · 캐릭터 · 배우. 아이돌에 묶지 않는다
  subject_type  text not null,      -- 'idol' | 'virtual' | 'character' | 'actor'
  kind          text not null,      -- 'birthday_cafe' | 'popup'
  starts_on     date not null,
  ends_on       date not null,
  open_hours    text,
  perks         text,               -- 선착 n명, 컵홀더 등
  conditions    text,               -- 음료 1잔 등
  source_url    text,               -- 원문 링크. 출처 표기 필수
  trust         text not null,      -- 'official' | 'partner' | 'user' | 'parsed'
  created_at    timestamptz default now()
);
create index on event (starts_on, ends_on);

create table goods (
  id            uuid primary key,
  event_id      uuid references event(id),
  name          text not null,
  is_random     boolean default false,   -- 랜덤 포카는 품절 개념이 아님
  sort_order    int
);

create table stock_report (
  id            uuid primary key,
  goods_id      uuid references goods(id),
  state         text not null,      -- 'in_stock' | 'sold_out' | 'limited'
  reporter_id   uuid,
  reported_at   timestamptz default now(),
  lat           double precision,
  lng           double precision
);
create index on stock_report (goods_id, reported_at desc);

create table crowd_report (
  id            uuid primary key,
  place_id      uuid references place(id),
  wait_bucket   text not null,      -- 'none' | 'lt10' | 'lt30' | 'gt60'
  reporter_id   uuid,
  reported_at   timestamptz default now(),
  lat           double precision,
  lng           double precision
);
create index on crowd_report (place_id, reported_at desc);
```

### 설계 노트

- **`trust` 필드를 처음부터 넣는다.** 공식 / 제휴 / 사용자 / 자동파싱을 구분해야
  나중에 자동 수집 비중을 늘릴 수 있다
- **`subject`를 아이돌에 묶지 않는다.** 생카는 이미 버추얼 · 애니 캐릭터 · 배우로
  확장됐다. 지역 단위로 전부 담으려면 스키마가 열려 있어야 한다
- 제보 테이블의 `lat`/`lng`는 검증용이자 **밀도 측정용 원천 데이터**다

---

## 8. 기술 스택과 배포

배포는 **Vercel 단독**. 두 기능 모두 서버리스로 충분하다.

| 레이어 | 선택 | 비고 |
| --- | --- | --- |
| 프레임워크 | Next.js (App Router) | Vercel 네이티브 |
| DB | Supabase Postgres + PostGIS | 반경 쿼리 `ST_DWithin` 한 줄 |
| 인증 | Supabase Auth | 익명 세션 허용, 제보에 최소 마찰 |
| 스토리지 | Supabase Storage | 안내 이미지 업로드 |
| 이미지 파싱 | Claude API | 건당 수 원. X API보다 압도적으로 저렴 |
| 지도 | 카카오맵 or 네이버 지도 SDK | 국내 POI 정확도 |
| 크론 | **GitHub Actions** | Vercel Hobby 크론은 일 1회 제한 |

예상 초기 비용: **월 20~30달러 이내**.

### 제약과 해법

| 제약 | 해법 |
| --- | --- |
| Vercel 서버리스는 WebSocket 서버 불가 | **폴링으로 충분.** 재고 30초, 혼잡도 15초. 초 단위 정확도가 필요한 데이터가 아니다 |
| 함수 실행 시간 제한 | 이미지 파싱은 업로드 → 큐 → 비동기 처리로 분리 |
| 웹은 백그라운드 위치 추적 불가 | 브릿지는 포그라운드 제보만 쓰므로 무관. **본 서비스(실시간 매칭) 단계에서 네이티브 전환 필요** |

### API 라우트

```
GET  /api/events?lat&lng&radius&date     반경 내 이벤트 조회
GET  /api/places/:id                     장소 상세 + 최신 혼잡도
GET  /api/events/:id/goods               굿즈 목록 + 품목별 최신 재고
POST /api/reports/crowd                  대기 제보 (GPS 200m 검증)
POST /api/reports/stock                  재고 제보 (GPS 200m 검증)
POST /api/submissions                    이벤트 제보 (링크 or 이미지)
```

---

## 9. 마일스톤

| 단계 | 기간 | 산출물 | 완료 기준 |
| --- | --- | --- | --- |
| **M0 데이터 시드** | 2주 | 홍대 · 합정 생카 대관 카페 200곳 리스트, T1 멤버 86명 마스터 | 장소 200 · 아티스트 86 등록 |
| **M1 조회** | 3주 | 지도 + 리스트, 반경 조회, 이벤트 상세 | 실 데이터로 오늘 일정 조회 가능 |
| **M2 제보** | 2주 | 재고 · 혼잡도 제보, GPS 검증, 신선도 감쇠 | 내부 테스트 제보 정상 집계 |
| **M3 현장 검증** | 1주말 | 타깃 생일 주간 실전 투입 | 아래 지표 참조 |
| **M4 확장** | 4주 | 성수 구역, 제보 등록(F5), 상위 50곳 대관 캘린더 제휴 | 커버리지 2구역 |
| **M5 점화** |, | 포카 교환 UI 오픈 | **밀도 임계 도달 시에만** |

### M3 실행안, 한 구역, 한 주말, 한 팬덤

1. T1 아티스트의 생일 주간을 하나 선택
2. **홍대 · 합정 반경 1km만** 오픈. 그 안 생카 30~50곳을 발로 커버
3. 주최자에게 QR 배포, "대기 현황 여기서 확인" (주최자도 홍보 이득이라 협조가 잘 된다)
4. 팀원이 현장에서 직접 부트스트랩 제보 (초기 빈 지도 방지)

---

## 10. 성공 지표

브릿지의 성패는 **다운로드 수가 아니라 좌표당 밀도**로 판단한다.

| 지표 | M3 목표 | 왜 |
| --- | --- | --- |
| **반경 1km 동시 활성 사용자** | **100명** | 교환 점화의 임계값 |
| 이벤트당 제보 수 | 5건 이상 | 지도가 비어 보이지 않는 최소선 |
| 제보 신선도 중앙값 | 15분 이내 | 이보다 오래되면 정보 가치 소멸 |
| 현장 체류 중 세션 길이 | 3분 이상 | 교환 전환의 선행 지표 |
| 다음 주말 재방문률 | 30% | 브릿지가 습관이 됐는지 |

**임계 도달 전에는 교환 탭을 노출하지 않는다.** 이 규칙을 지표보다 우선한다.

---

## 11. 리스크

### 안전, 나중에 붙이는 기능이 아니다

본 서비스는 **모르는 사람과 오프라인에서 만나는 구조**이고,
주 사용자층에 미성년자와 여성 비중이 매우 높다.

생카 · 팝업 현장을 브릿지로 잡는 것이 여기서도 유리한데,
교환이 **공개된 장소와 공개된 시간대**에 자연스럽게 묶이기 때문이다.
**"지정 안전 거래 장소 = 지금 이벤트가 열리는 그 카페"를 기본값으로 강제**하는 설계를
첫 버전부터 넣는다. 임의 좌표 만남을 허용하지 않는다.

### 그 외

| 리스크 | 대응 |
| --- | --- |
| 제보 밀도 부족으로 빈 지도 | 팝업 3~5곳으로 시작. 20곳 제보 0건보다 3곳 제보 30건이 낫다 |
| 허위 · 장난 제보 | GPS 200m 검증 + 동일 시간대 제보 간 교차 검증 + 신뢰도 감쇠 |
| 계절성 (컴백 주기 의존) | 공방(주 5회 고정)을 조기에 붙여 비수기 리텐션 방어 |
| 카페 협조 실패 | M0에서 조기에 판명됨. 50곳 미만이면 발품 + UGC 비중으로 전환 |
| 오프메이트 대응 | 리스팅으로 정면 승부하지 않는다. 축이 다르면 같은 데이터로도 다른 제품이다 |
| 웹의 한계 | 매칭 단계에서 네이티브 전환 전제. 브릿지 단계는 웹으로 검증만 |

---

## 12. 미결 사항

- [ ] 지도 SDK 선택, 카카오 vs 네이버 (POI 정확도 · 쿼터 비교 필요)
- [ ] 카페 제휴 조건 확정, 무료 노출인가, 대관 리드 제공까지인가
- [ ] 익명 제보 허용 범위, 가입 없이 제보 가능하게 할지 (마찰 vs 어뷰징)
- [ ] M3 타깃 아티스트 · 날짜 확정
- [ ] 랜덤 포카 현황 UI 형태, 이게 교환 전환의 핵심 동선이라 별도 설계 필요

---

## 부록, 참고 수치

| 수치 | 값 | 출처 |
| --- | --- | --- |
| 홍대 · 합정 하루 생카 수 | 100곳 이상 | 오프메이트 집계 / 헤럴드경제 |
| 인기 아이돌 한 구역 동시 개최 | 30곳 이상 | 캐릿 |
| 생카 대관 예약금 | 10~50만 원 | 헤럴드경제 |
| 더현대 서울 연간 K-pop 팝업 | 30회 | 현대백화점 |
| 틱톡 생일카페 후기 조회수 | 1,400만+ | 캐릿 |
| 연간 K-pop 트윗 | 78억 (2021) | X 공식 |

수요 추정치는 공개 리포트 · 집계 서비스를 프록시로 삼은 것이며,
실측 전까지 전부 가설로 다룬다.
