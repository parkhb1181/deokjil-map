# 덕질 현장 지도, 수요 검증 PoC

> **한 줄 정의**
> 서울에서 열리는 생카·팝업을 위치·날짜로 모아 보여주고,
> **흩어진 공식 품절·대기 공지를 그 위에 시계열로 얹는** 웹.
> 목적은 서비스 출시가 아니라 **수요 측정** 하나다.

작성일 2026-08-27 · 내부 기획 문서 · 전체 구상은 [bridge-plan-full.md](bridge-plan-full.md)

---

## 0. 이 문서의 위치

`bridge-plan-full.md`는 브릿지 제품 전체 구상이고,
본 문서는 그중 **조회 + 제보**를 2일 안에 만들고 1주 관측하는 PoC 계획이다.

전체 구상의 목표는 *좌표당 사용자 밀도*였다. PoC의 목표는 그 앞단이다.

> **"이 정보를 모아 보여주는 웹에, 사람이 오고 · 머무르고 · 돌아오는가."**

### 왜 조회만으로는 안 되는가

조회 기능만 만들면 기존 서비스(오프메이트 등)와 **같은 축**이 된다.
그건 전체 구상 1번에서 이미 기각한 형태이고, 기각한 것을 만드는 셈이 된다.

특히 **재방문**이 성립하지 않는다. 정적 리스트는 한 번 보고 코스 짜면 끝이다.
반면 재고·대기는 정보가 계속 바뀌므로 돌아올 이유가 생긴다.
**지표 5(7일 재방문)를 만들 수 있는 유일한 기능이 F2·F3다.**

그래서 F2·F3를 PoC 범위에 포함하고, **홍보 훅도 여기서 가져간다.**

---

## 1. 검증 가설과 판정 기준

콘텐츠 **정합성은 이번 검증 대상이 아니다.**

표본이 작으면 재방문률은 통계적으로 무의미하므로, **유입 규모를 1차 관문**으로 두고
통과했을 때만 뒤 지표를 해석한다.

| 순서 | 지표 | 기준 | 의미 |
| --- | --- | --- | --- |
| 0 | 방문 수 | **300 이상** | 미달 시 뒤 지표 해석 금지. 유입 재시도 |
| 1 | 체류 중앙값 | **90초 이상** | 조회 제품으로선 충분히 긴 편 |
| 2 | 상세 진입률 | **40% 이상** | 리스트만 보고 나가면 제품이 얕다는 뜻 |
| 3 | **담기 발생률** | **15% 이상** | ★ 소비 측 핵심 지표 |
| 4 | **제보 완료율** | **3% 이상** | ★ 기여 측 핵심 지표 |
| 5 | 7일 재방문 | **20% 이상** | 전체 구상의 30%는 현장 앱 기준이라 조회 웹엔 과함 |

**3번과 4번은 성격이 다르다.** 담기는 소비 행동이고, 제보는 **기여 행동**이다.
제보가 일어나면 전체 구상의 UGC 가설이 성립한다는 조기 신호가 된다.

4번 기준값(3%)은 근거 있는 목표가 아니라 **관측 시작점**이다.
방문자 대부분이 현장이 아니라 집에서 들어오므로 절대값보다 *발생 여부*가 중요하다.

### 정합성 교란 방어

정합성을 검증하진 않되, 그것이 재방문 지표를 오염시키는 것은 막아야 한다.
틀린 정보로 헛걸음한 사용자는 지표에서 사라지고,
그러면 낮은 재방문이 *제품 매력도* 때문인지 *데이터 품질* 때문인지 구분할 수 없다.

- 모든 카드에 **원문 링크 필수 노출** (전체 구상 5.4 가드레일)
- "**주최자 공지 기반 · 방문 전 원문 확인 권장**" 상시 표기
- **원문 링크 클릭률(`open_source`) 계측**, 신뢰도의 대리 지표
- 제보에 **신선도 표기와 감쇠**를 처음부터 적용 (4.3 참조)

---

## 2. 범위

### 만드는 것

| | 내용 |
| --- | --- |
| 조회 | 지도 + 리스트 토글, 지역·날짜·아티스트 필터 |
| 상세 | 대상, 유형, 장소, 기간, 특전, 조건, **원문 링크** |
| **F2 재고** | 팝업 한정. 품목별 3-state. **공식 품절 공지 시드** + 사용자 제보 |
| **F3 혼잡도** | 한 탭 제보(`대기 없음`/`10분`/`30분`/`1시간+`), 핀 색상, 신선도 감쇠. 팝업 일부만 시드 있음 |
| 담기 | localStorage 코스 저장, 내 코스 탭 |
| 계측 | 이벤트 9종, CF Analytics + GA4 |

화면은 넷이다. **지도/리스트 · 이벤트 상세 · 내 코스 · 제보 시트.**

### 만들지 않는 것

| 전체 구상 | PoC | 사유 |
| --- | --- | --- |
| F4 GPS 검증 (게이트) | **게이트 해제** | 3.3 참조. 판정 결과는 플래그로 저장 |
| F5 제보 등록 | 제외 | 커버리지는 사전 수집으로 채운다 |
| F6 포카 교환 | 제외 | 코드에도 넣지 않는다 |
| 로그인 | 제외 | 익명. localStorage UUID |
| M0~M5 (12주) | **2일 + 1주 관측** | |
| 밀도 지표 (반경 1km 100명) | **접속·체류·재방문·제보** | 검증 축이 다름 |
| 현장 실사 | 제외 | 정합성 비검증 |

### 이월 (PoC 범위 외, 폐기 아님)

전체 구상 11번의 **안전 설계**(공개 장소 강제, 임의 좌표 만남 금지)는
교환 기능이 없는 PoC에선 해당 사항이 없다. 다만 본 서비스 진입 시
**첫 버전부터 필수**이므로 삭제하지 않고 이월로 표기한다.

---

## 3. 콜드스타트 설계

F2·F3를 넣을 때 시간보다 큰 리스크는 **빈 화면**이다.

> "지금 가면 살 수 있나?" → "제보 없음"
> "대기 얼마나?" → "제보 없음"

차별점으로 홍보한 기능이 전부 빈칸이면 그건 차별점이 아니라 **결함**으로 읽힌다.
전체 구상 11번의 "빈 지도" 리스크가 F2·F3에서 재발하는 것이다. 세 겹으로 막는다.

### 3.1 시드는 공식 공지만, 목격담도 창작도 넣지 않는다

시드로 넣는 것은 **주최자 · 운영사 · 기획사의 공식 공지**뿐이다.

| 넣는다 | 안 넣는다 |
| --- | --- |
| "아크릴 스탠드 **품절**되었습니다" (운영사 공지) | "지금 20명 남았어요" (팬 목격담) |
| "금일 **웨이팅 마감**" (팝업 공식) | "웨이팅 30분쯤 되는 듯" (일반 트윗) |
| "선착 특전 **소진**" (주최자 공지) | 우리가 지어낸 값 |

일반 사용자의 현장 목격담은 **출처를 검증할 수 없고 책임 주체가 없다.**
공식 공지는 발신 주체와 시각이 명확해 `source_url`로 그대로 검증된다.
이 원칙이 4.4 법적 가드레일 및 1번 정합성 교란 방어와 같은 선상에 있다.

**결과, 시드는 팝업에 몰린다.**

| | 재고 시드 | 대기 시드 |
| --- | --- | --- |
| **팝업** (3~8곳) | 공식 품절 공지 **있음** | 웨이팅 마감 공지 **일부** |
| **생카** (100곳+) | 해당 없음 (굿즈 없음) | **없음**, 주최자가 대기를 공지하지 않음 |

즉 **생카의 대기 현황은 런칭 시점에 전부 빈칸**이다.
이 사실이 8번 홍보 훅과 9번 우선순위를 바꾼다.

**시각 처리 규칙, 이게 핵심이다.**

- `reported_at`은 **원문 게시 시각**을 그대로 넣는다. `now()`로 찍지 않는다
- `trust = 'parsed'`, `source_url` 필수. 화면에 출처 표기
- 결과적으로 신선도 감쇠(4.3)에 자연히 걸려 "**2시간 전 정보**"로 흐리게 표시된다

즉 시드는 "지금 재고 있음"을 주장하지 않고 "**N시간 전엔 이랬다**"를 보여준다.
정확하면서 화면도 채운다. `now()`로 찍는 순간 수집이 아니라 조작이 되고,
F3의 가치인 신선도 자체가 무너진다.

### 3.2 빈 상태를 참여 유도로

제보가 없는 항목은 "정보 없음"이 아니라
"**아직 제보가 없어요 · 첫 제보 남기기**"로 표시한다.
빈칸을 결함이 아니라 참여 유도 지점으로 프레이밍한다.

### 3.3 GPS 게이트는 끄되 판정은 기록한다

전체 구상 F4는 반경 200m 밖에서 제보 버튼을 비활성화한다.
PoC에서 이걸 켜면 **커뮤니티에서 유입된 방문자 대부분이 버튼을 누를 수 없고**,
지표 4(제보 완료율)를 측정할 방법이 사라진다.

- 제보는 **누구나 가능**
- RPC가 200m 판정만 하여 `in_range` **플래그로 저장**
- 화면에서는 현장 제보에만 **`현장 확인` 배지**

지표도 얻고 신뢰도 구분도 남는다. 게이트 자체는 본 서비스에서 되살린다.

---

## 4. 데이터

### 4.1 이벤트 수집, 반자동 파이프라인

2일 안에 자동 크롤러는 무리다.

```
① 소스에서 링크·이미지 수집        (수동)
        ↓
② 안내 이미지·텍스트 → 구조화 JSON  (Claude, 로컬 1회 배치)
        ↓
③ 주소 → 좌표 지오코딩              (카카오 로컬 API, 무료)
        ↓
④ events.json 커밋                  (빌드타임 번들)
```

②는 전체 구상 F5(런타임 파싱)와 다르다. **빌드타임 1회 배치**라 운영비 영향이 없다.

| 순위 | 소스 | 비고 |
| --- | --- | --- |
| 1 | 생카 큐레이션 계정 (X · 인스타) | **한 게시물에 10~20건** |
| 2 | 생카 대관 카페 계정 | 고정 사업자라 계정이 안 바뀜 |
| 3 | 커뮤니티 정리글 | 텍스트라 파싱이 가장 쉬움 |
| 4 | 기획사 · 유통사 공식 채널 | 팝업. 월 10~20건이라 수기로 충분 |

**제보 시드 소스는 별도다.** 이벤트 수집이 큐레이션 계정까지 허용하는 것과 달리,
재고·대기 시드는 **운영사 · 주최자 · 기획사 공식 계정만** 인정한다 (3.1).

### 4.2 커버 범위

| | 브릿지 원안 | PoC 목표 | 컷라인 시 |
| --- | --- | --- | --- |
| 지역 | 홍대·합정 → 성수 | **서울 전역** | 홍대·합정·성수 3구역 |
| 기간 | 특정 주말 | **오늘부터 2주** | 동일 |
| 건수 | 30~50 | **150~300** | 80~100 |
| 아티스트 | 화이트리스트 없음 | 동일 (검색·필터용으로만) | 동일 |

**체류 시간은 볼 게 얼마나 많으냐의 함수**다. 30건짜리 지도로 90초를 채울 수 없다.

### 4.3 신선도 감쇠

전체 구상 F3의 규칙을 그대로 적용한다. **크론 없이 읽기 시점에 계산**한다.

| 경과 | 표시 |
| --- | --- |
| ~10분 | 진하게 + `방금` |
| 10~30분 | 보통 + `N분 전` |
| 30분~ | **흐리게** + `N분 전`, 오래된 정보는 없는 정보보다 나쁘다 |
| 24시간~ | 미표시 |

### 4.4 법적 가드레일, 유지

정합성을 안 따진다고 해서 여기를 타협하지 않는다. 되돌릴 수 없는 항목이다.

- 타 리스팅 서비스 무단 크롤링 금지
- **안내 이미지 자체를 복제 게시하지 않는다.** 사실 정보만 재구성 + 원문 링크
- X API 미사용 (전체 구상 5.3). 필요 시 `publish.twitter.com/oembed`만

---

## 5. 데이터 스키마

읽기 데이터와 쓰기 데이터를 분리한다.

```
events.json (+ goods)        →  리포지토리 커밋, 빌드타임 번들   [읽기]
crowd_report / stock_report  →  Supabase                        [쓰기]
```

150~300건짜리 반경·날짜 필터는 브라우저 배열 순회가 더 빠르다.

### 5.1 events.json

```jsonc
[
  {
    "id": "evt_0001",
    "place": {
      "name": "카페 ○○",
      "address": "서울 마포구 ...",
      "lat": 37.5563, "lng": 126.9236,
      "district": "hongdae",          // hongdae | seongsu | gangnam | ...
      "kind": "cafe"                  // cafe | popup_venue
    },
    "subject": "○○○",
    "subject_type": "idol",           // idol | virtual | character | actor
    "kind": "birthday_cafe",          // birthday_cafe | popup
    "starts_on": "2026-08-29",
    "ends_on": "2026-08-31",
    "open_hours": "12:00~20:00",
    "perks": "선착 100명 컵홀더",
    "conditions": "음료 1잔 주문",
    "source_url": "https://...",      // 필수. 노출 필수
    "trust": "parsed",                // official | partner | user | parsed
    "goods": [                        // 팝업만. 생카는 빈 배열
      { "id": "gd_01", "name": "아크릴 스탠드",  "is_random": false, "sort_order": 1 },
      { "id": "gd_02", "name": "랜덤 포토카드", "is_random": true,  "sort_order": 2 }
    ]
  }
]
```

전체 구상 7번의 Postgres 필드를 **그대로 유지**한다.
`trust` · `subject_type` · `source_url`은 PoC에서 안 써도 남긴다 
승격 시 매핑 없이 넘기기 위함이다.
`place`를 별도 엔티티로 안 쪼갠 이유는 PoC에 장소 재사용 조회가 없어서다.

### 5.2 Supabase, 테이블 2개, RPC 2개

```sql
create table crowd_report (
  id           uuid primary key default gen_random_uuid(),
  place_id     text not null,          -- events.json의 place 참조 (문자열 키)
  wait_bucket  text not null,          -- 'none' | 'lt10' | 'lt30' | 'gt60'
  reporter_id  text,                   -- localStorage UUID
  reported_at  timestamptz not null default now(),   -- 시드는 원문 게시 시각
  lat          double precision,
  lng          double precision,
  in_range     boolean default false,  -- 200m 판정 결과 (게이트 아님, 기록용)
  trust        text not null default 'user',   -- 'user' | 'parsed'
  source_url   text                    -- trust='parsed'일 때 필수
);
create index on crowd_report (place_id, reported_at desc);

create table stock_report (
  id           uuid primary key default gen_random_uuid(),
  goods_id     text not null,
  state        text not null,          -- 'in_stock' | 'sold_out' | 'limited'
  reporter_id  text,
  reported_at  timestamptz not null default now(),
  lat          double precision,
  lng          double precision,
  in_range     boolean default false,
  trust        text not null default 'user',
  source_url   text
);
create index on stock_report (goods_id, reported_at desc);
```

RPC 2개(`report_crowd`, `report_stock`)가 전체 구상 8번의 API 라우트를 대체한다.
반경 판정은 **PostGIS 없이** 하버사인으로 계산한다. 장소 좌표를 인자로 받으면
DB에 장소 테이블이 없어도 된다.

```sql
create function report_crowd(
  p_place_id text, p_bucket text, p_reporter text,
  p_lat float8, p_lng float8, p_place_lat float8, p_place_lng float8
) returns void language plpgsql security definer as $fn$
declare v_in_range boolean := false;
begin
  if p_lat is not null then
    -- 하버사인 근사. 200m 이내 여부만 본다
    v_in_range := (
      6371000 * acos(least(1, greatest(-1,
        cos(radians(p_lat)) * cos(radians(p_place_lat))
        * cos(radians(p_place_lng) - radians(p_lng))
        + sin(radians(p_lat)) * sin(radians(p_place_lat))
      ))) <= 200
    );
  end if;
  insert into crowd_report(place_id, wait_bucket, reporter_id, lat, lng, in_range)
  values (p_place_id, p_bucket, p_reporter, p_lat, p_lng, v_in_range);
end $fn$;
```

**어뷰징 방어**, 서버가 없어 IP 레이트리밋을 못 건다.
`reporter_id` 기준 **동일 장소 10분 내 재제보 금지**를 트리거로 건다.
PoC는 표본이 작아 이 정도로 충분하다.

---

## 6. 기술 스택

| 레이어 | 선택 | 비용 |
| --- | --- | --- |
| 프레임워크 | Vite + React (정적 빌드) | $0 |
| 호스팅 | **Vercel** (Hobby) | $0 |
| 쓰기 저장소 | **Supabase Free** (테이블 2 · RPC 2) | $0 |
| 지도 | **카카오맵 JS SDK** | $0 (일 30만 호출) |
| 지오코딩 | 카카오 로컬 API | $0 · 지도와 **같은 키** |
| 코스 저장 | localStorage | $0 |
| 애널리틱스 | Vercel Analytics + GA4 + Clarity | $0 (전부 무료 티어) |
| 파싱 | Claude (빌드타임 1회) | 수백 원 |

지도 SDK는 **카카오로 확정**. 무료 쿼터가 넉넉하고 카드 등록이 없으며,
지오코딩이 같은 키로 붙어 파이프라인 ③이 공짜로 해결된다.
네이버는 무료 쿼터 밖 과금이 있어 "비용 0" 원칙과 충돌한다.

제외: PostGIS · Supabase Auth · Storage · 크론 · Next.js API 라우트.

계측 스크립트는 `afterInteractive` 로 넣는다. 계측이 첫 렌더를 늦추면
그 자체가 이탈을 만들어 재려던 지표를 왜곡한다.

**갱신 방식**, 전체 구상 8번은 폴링(재고 30초/혼잡도 15초)이었으나
그 근거였던 "서버리스 WebSocket 불가" 제약이 Supabase Realtime으로 사라진다.
Realtime 구독을 쓰면 무변경 구간 트래픽이 0이 되어 Free egress(5GB/월)에 여유가 생긴다.
**단 이건 P4다.** 컷되면 화면 진입 시 1회 조회로 충분하다.

**카카오 JS 키 도메인 등록**, 와일드카드는 비즈앱 전용이라 일반 앱은 URL을 개별 등록한다.
`.vercel.app` 서브도메인은 전역 고유라 선점 가능성이 있으므로 **1회 배포 후 실제 URL을 확인하고 등록**한다.
배포별 해시 URL(프리뷰)은 등록이 불가능하므로 **프리뷰에서는 지도가 뜨지 않는다** 
지도 컴포넌트는 키 부재·도메인 불일치 시 리스트로 폴백시킨다.

Free 티어 주의, **7일 무활동 시 프로젝트 일시정지.** 관측 기간엔 트래픽이 있어 무관하나,
배포 전 웜업을 확인한다.

---

## 7. 계측 설계

이번 PoC에선 **계측이 곧 제품**이다. 성격이 달라 툴 두 개를 병행한다.

| 툴 | 담당 | 특성 |
| --- | --- | --- |
| **Vercel Analytics** | 방문자 · 유입 경로 | 자사 도메인에서 전송 → 애드블록에 덜 막힘 → **모수 신뢰**. 커스텀 이벤트는 쏘지 않는다 (Hobby 한도) |
| **GA4** | 체류 · 재방문 · 커스텀 이벤트 | 이벤트 한도가 넉넉하다. 애드블록에 일부 차단되므로 상대 비교용 |
| **Microsoft Clarity** | 세션 리코딩 · 히트맵 | 표본 300 수준에서는 "왜 이탈했나"를 숫자로 알 수 없다. 눈으로 보는 수단 |

**Clarity 를 넣은 이유가 중요하다.** 지표 1·2(체류·상세 진입률)가 낮게 나왔을 때,
숫자만으로는 콘텐츠가 얕은 것인지 UI 가 막힌 것인지 구분할 수 없다.
n=300 에서는 정성 관찰의 정보량이 정량 지표보다 크다.
세션 리코딩이므로 텍스트 마스킹을 켜고 푸터에 고지한다.

### 이벤트 9종, 이 이상 만들지 않는다

| 이벤트 | 시점 | 대응 지표 | 우선순위 |
| --- | --- | --- | --- |
| `view_home` | 진입 | 방문 수 | P0 |
| `view_detail` | 이벤트 상세 진입 | 상세 진입률 | P0 |
| `open_source` | 원문 링크 클릭 | 신뢰도 대리 지표 | P0 |
| `return_visit` | 재방문 | 7일 재방문률 | P0 |
| `report_open` | 제보 시트 열기 (재고·대기 공통) | 기여 의도 | P1 |
| `report_submit` | 제보 완료 (`kind`로 재고/대기 구분) | ★ 제보 완료율 | P1 |
| `save_course` | 담기 | ★ 담기 발생률 | P2 |
| `filter_change` | 필터 조작 | 탐색 깊이 | P4 |

`report_open` / `report_submit`을 재고·대기로 쪼개지 않고 `kind` 파라미터로 구분한다.
P1에서 두 기능이 한 컴포넌트로 묶이므로(9번) 계측도 같이 묶는 게 맞다.

`return_visit`은 **GA4 기본 재방문 지표를 그대로 믿지 않는다.**
쿠키 만료·시크릿창 때문에 과소 계상되므로,
localStorage에 최초 방문일(`first_seen`)을 심어 함께 전송한다.

---

## 8. 유입 설계, 여기가 진짜 병목

만들어놓고 기다리면 방문자는 0에 수렴한다. **2일 중 반나절을 유입에 배정**한다.

| 경로 | 기대 | 비고 |
| --- | --- | --- |
| **커뮤니티 1회 포스팅** | 수백~수천 | 링크 하나로 규모가 나오는 유일한 채널 |
| **X 검색 유입** | 수십 | 수는 적지만 의도가 명확한 트래픽 |
| **OG 이미지** |, | 없으면 커뮤니티 클릭률 반토막. 구현 30분, ROI 최상 |

채널별로 나눠 쏘고 **UTM을 분리**한다. 채널별 체류·재방문 차이가 곧 타깃 정보다.

### 홍보 훅, 시드가 있는 쪽으로 건다

리스팅으로 홍보하면 기존 서비스와 구분되지 않는다. 차별점에서 훅을 가져와야 하는데,
**빈 화면은 훅이 될 수 없다.** 3.1에 따라 런칭 시점에 실제로 채워지는 건 **팝업**뿐이다.

> **"팝업 굿즈 뭐가 품절됐는지, 공식 공지 모아서 한 화면에."**

이게 정직하면서 강하다. 공식 계정 수십 개에 흩어진 품절 공지를
**한 곳에 시계열로 모아 보여주는 서비스가 없다.** 그 자체가 차별점이다.

생카 대기(F3)는 훅으로 쓰지 않는다. 빈 화면을 훅으로 걸면
방문자가 첫 화면에서 이탈하고, 그게 체류·재방문 지표를 통째로 망친다.
대신 **"직접 제보하기"**를 부가 메시지로 얹어 지표 4를 연다.

이 판단 때문에 9번 우선순위에서 **F2가 F3보다 위**에 온다.

---

## 9. 일정과 우선순위

**계획은 완전판. 다만 8/28(금) 저녁 배포는 고정이다.**
그때까지 못 끝낸 항목은 빼고 배포한다. 일정을 늘리지 않는다.

하루 밀리면 관측 주말의 절반을 잃는다. 생카도 커뮤니티 유입도 주말에 몰리므로,
그 손실이 기능 하나를 더 넣는 이득보다 크다.

### 우선순위 스택, 위에서부터 채운다

| | 항목 | 예상 | 컷되면 |
| --- | --- | --- | --- |
| **P0** | 지도/리스트 토글, 지역·날짜 필터, 이벤트 상세, 원문 링크, 계측 4종, OG 이미지, 배포 | 5h | **배포 자체를 안 한다** |
| **P1** | **팝업 현장 상황**, F2 재고 + F3 대기를 팝업 상세 한 화면에. 공식 시드 표시, 신선도 감쇠, `trust` 배지, 제보 시트, 계측 2종 | 5h | **홍보 훅 상실.** 리스팅 사이트로 전락 |
| **P2** | 담기 / 내 코스 + `save_course` | 2h | 지표 3 상실, 재방문 훅 약화 |
| **P3** | **F3를 생카 전체로 확장**, 핀 색상 + 제보 그릇 | 1.5h | 지표 4 표본 축소 (팝업분은 남음) |
| **P4** | Realtime 구독, `filter_change`, `report_stock_submit` 분리 계측 | 3h | 영향 미미 |

P0~P2 = 12시간이 **현실적 목표선**이다. P3 이하는 여유가 나면 얹는다.

> **P1에서 F2·F3를 묶은 이유**, 신선도 표시, `trust` 배지, 제보 시트, 감쇠 로직이
> 두 기능에 공통이다. 따로 만들면 4h + 5h지만 팝업 상세 한 화면으로 묶으면 5h다.
>
> **F2가 F3보다 위인 이유**, 3.1의 귀결이다. 공식 공지만 쓰면 재고는 채워지고
> 생카 대기는 빈다. **채워지는 쪽이 훅이 되어야 한다.**
>
> **P2(담기)가 P3(생카 F3)보다 위인 이유**, 생카 F3는 런칭 시 전부 빈칸이라
> 그날의 체류에 기여하지 못한다. 담기는 2시간에 지표 3과 재방문 훅을 동시에 준다.

### Day 1, 8/27 (목) · 기획 + 데이터

| | 작업 | 우선순위 |
| --- | --- | --- |
| 오전 | 기획 확정, `events.json` 스키마 고정, 계측 이벤트 정의 |, |
| 오후 | 소스 링크 수집 → Claude 파싱 → 이벤트 150건 | P0 |
| 오후 | **팝업 3~8곳 굿즈 마스터 입력** (~45~120품목) | **P1** |
| 저녁 | 지오코딩 + **좌표 육안 검수** | P0 |
| 저녁 | **공식 품절·웨이팅 공지 시드 수집** (3.1 규칙) | **P1** |

좌표는 육안 검수한다. **좌표가 틀리면 지도 제품은 통째로 죽는다** 
정합성 비검증 원칙의 유일한 예외다.

굿즈 마스터가 P1로 올라왔다. **팝업이 홍보 훅을 지탱하므로 여기가 가장 비싼 시드다.**
팝업 수가 적어(3~8곳) 품목 입력이 감당 가능한 규모라는 점이 이 구조를 성립시킨다.
반대로 생카 100곳에 굿즈를 붙이려 했다면 이틀이 통째로 날아갔을 것이다.

### Day 2, 8/28 (금) · 구현 + 배포

| | |
| --- | --- |
| 오전 | **P0** 완주 |
| 오후 | **P1 → P2 → P3** 순으로 채움 |
| **18:00** | **하드 컷.** 미완 항목 제거, 배포 |
| 저녁 | 모바일 실기 확인 → **유입 포스팅 실행** (채널별 UTM) |

### 8/29 ~ 9/5 · 관측

**이 기간엔 아무것도 고치지 않는다.** 코드·데이터 모두 동결.
고치면 지표가 오염되어 이틀 작업이 해석 불가능한 숫자가 된다.

---

## 10. 확정 사항

전체 구상 12번 미결 중 PoC 진행을 막는 것들을 확정한다.

| 항목 | 결정 |
| --- | --- |
| 지도 SDK | **카카오**, 무료 쿼터 + 지오코딩 동일 키 |
| 익명 허용 범위 | **전면 익명**, 로그인 없음. 가입 마찰은 표본을 0으로 만든다 |
| GPS 검증 | **게이트 해제, 판정은 `in_range` 기록** (3.3) |
| 제보 시드 | **공식 공지만.** 팬 목격담·창작 데이터 금지. 원문 게시 시각 유지, 출처 표기 (3.1) |
| 홍보 훅 | **팝업 굿즈 품절 현황** (F2). 생카 대기는 빈 화면이라 훅으로 안 씀 (8번) |
| 타깃 아티스트 | **지정하지 않음**, 그날 열리는 것을 전부 담는다 |
| 커버 지역 | 서울 전역 (컷라인 시 3구역) |
| 배포 시점 | **8/28 금 18:00 하드 컷** |
| 카페 제휴 · 랜덤 포카 UI | **PoC 범위 외** |

---

## 11. 리스크

| 리스크 | 대응 |
| --- | --- |
| **유입 0 → 지표 없음** | 최대 리스크. 반나절 배정 + 채널 분산 + OG 이미지 |
| **팝업 화면 빈칸** | 3번 3중 방어. 공식 품절 공지가 시드로 들어가므로 여기는 채워진다 |
| **생카 대기 전면 빈칸** | 공식 소스 부재로 불가피. **훅에서 제외**하고 참여 유도로만 노출 (8번) |
| 공식 품절 공지마저 부족 | Day 1 오후에 조기 판명. 15건 미만이면 **P1 훅을 재고→"공식 라인업 모아보기"로 축소** |
| 시드 제보가 신선도를 위장 | **`now()` 금지, 원문 시각 유지.** 감쇠에 걸려 흐리게 표시되는 게 정상 |
| 팬 목격담 유입 유혹 | 시드는 공식만. 사용자 제보는 `trust='user'`로 **화면에서 분리 표시** |
| 시간 초과로 P1 미완 | 9번 우선순위 스택대로 하위부터 절단. 배포일은 안 민다 |
| 데이터 오류로 재방문 오염 | 원문 링크 필수 + 미검증 표기 + `open_source` 계측 |
| 좌표 오류 | Day 1 저녁 육안 검수 (유일한 정합성 예외) |
| 표본 부족으로 해석 불가 | 방문 300을 1차 관문으로. 미달 시 뒤 지표 해석 금지 |
| 제보 어뷰징 | `reporter_id` 기준 10분 재제보 금지 트리거. 표본이 작아 이 정도로 충분 |
| 관측 중 수정 유혹 | 8/29~9/5 코드·데이터 동결 |
| 저작권 · 크롤링 | 4.4 가드레일 유지 |

---

## 12. PoC 이후

| 결과 | 해석 | 다음 |
| --- | --- | --- |
| **전 지표 통과** | 브릿지 가설 성립 | 전체 구상 M4(성수 확장·제휴)로 직행. 스키마를 7번 Postgres로 승격 |
| 제보만 발생 | 기여 의지 확인, 소비 훅 약함 | 조회 UX 재설계 |
| 담기만 발생 | 조회 수요는 있으나 UGC 미성립 | 데이터 수집 자동화에 투자, UGC 의존도 축소 |
| 유입 미달 | 제품이 아니라 채널 문제 | 유입 경로 변경 후 재시도 |
| 체류·재방문 동시 미달 | 브릿지 가설 자체 재검토 | 1번 후보 재평가 |
