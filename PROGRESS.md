# 덕모임 인수인계 문서

> **주의: 이 문서는 오래됐다.** 2026-08-27 기준이고 그 뒤로 96커밋이 지났다.
> 여기 적힌 것 중 **P0·데이터·크롤러·계측 절은 아직 유효하고**, 화면과
> 우선순위 절은 그렇지 않다.
>
> 그 뒤에 브랜드가 덕모임으로, 도메인이 duckmoim.com 으로 바뀌었고
> 담기·순위 레일·이미지 최적화·아티스트 딥링크가 붙었다. 그리고 **1차
> MVP 화면(모집글·프로필·로그인·백오피스)이 통째로 새로 생겼는데 이
> 문서에는 한 줄도 없다.**
>
> | 지금 볼 문서 | 무엇이 있나 |
> |---|---|
> | **[docs/FRONTEND.md](docs/FRONTEND.md)** | **화면 작업은 여기부터.** 만든 화면, 명세에서 뺀 것, 미결 항목, 아직 확인 못 한 것 |
> | [docs/design/SCALE.md](docs/design/SCALE.md) | 크기·간격·목록 배치의 기준과 그 근거 |
> | [docs/design/BRIEF.md](docs/design/BRIEF.md) | 디자인 브리프. 실제 콘텐츠의 모양 |

작성 2026-08-27 · 기준 커밋 `5a584dd` · 배포 https://duckmoim.com

| 문서 | 역할 |
|---|---|
| **PROGRESS.md** (이 문서) | 어디까지 했고 다음에 뭘 하는지 |
| [poc-plan.md](poc-plan.md) | 지금 만드는 것의 기획. **섹션 번호로 계속 참조하니 먼저 읽을 것** |
| [bridge-plan-full.md](bridge-plan-full.md) | PoC 이후 전체 구상 |
| [CLAUDE.md](CLAUDE.md) | 코드·데이터 규칙. 어기면 안 되는 것들 |
| [crawler/README.md](crawler/README.md) | 크롤러 원칙 + robots.txt 확인 결과 |

---

## 0. 30초 요약

서울 생카·팝업을 **위치 축**으로 모아 보여주는 웹. 1주 수요 검증 PoC.
목적은 서비스 출시가 아니라 **"사람이 오고 · 머무르고 · 돌아오는가"** 측정 하나다.

**P0(조회 + 계측 + 배포)은 끝났다.** 162건이 지도와 리스트에 뜨고 계측 3종이 살아 있다.

**남은 것은 P1이고, 여기가 제품의 존재 이유 전부다.**
지금 상태는 poc-plan 0번이 명시적으로 기각한 형태, 조회만 있는 리스팅이다.
기존 서비스(오프메이트·팝플리)와 같은 축이고, 정적 리스트라 **재방문할 이유가 없다.**
지표 5(7일 재방문)를 만들 수 있는 기능이 P1뿐이다.

**그리고 P1의 순서를 바꿔야 한다.** 원래 계획은 굿즈 재고(F2)가 먼저였는데
그 데이터를 얻을 방법이 없다는 게 확인됐다. → **3번에서 혼잡도 우선을 제안한다.**

---

## 1. 개발 환경 세팅

### 1.1 실행

```bash
npm install
npm run dev          # localhost:3000 (포트 고정)
npm run dev:lan      # 같은 와이파이의 휴대폰에서 접속
npm run build        # 타입체크 + 빌드
npm run typecheck
```

**포트 3000은 바꾸면 안 된다.** 카카오 JS 키의 등록 도메인이 **포트까지 정확히**
일치해야 지도가 뜬다. `predev`가 `scripts/check-port.mjs`로 3000 점유를 검사해
다른 프로세스가 물고 있으면 실행을 막는다. Next가 조용히 3001로 넘어가서
"지도가 안 뜬다"로 30분을 태우는 걸 방지하는 장치다.

### 1.2 환경변수 (`.env.local`)

```
NEXT_PUBLIC_KAKAO_JS_KEY=...     # 카카오 개발자센터 > 앱 > JavaScript 키
KAKAO_REST_KEY=...               # 지오코딩 스크립트용 (REST API 키, 별개다)
NEXT_PUBLIC_GA4_ID=G-...
NEXT_PUBLIC_CLARITY_ID=...
```

- **JS 키와 REST 키는 다른 값이다.** JS 키를 REST 엔드포인트에 쓰면
  `401 KA Header is required`가 난다 (실제로 한 번 겪었다). `scripts/geocode.mjs`에
  이 진단 메시지를 넣어놨다
- 카카오 키가 없으면 지도가 **리스트로 폴백**한다. 에러가 아니라 정상 경로다
- 계측 ID가 없으면 스크립트를 아예 안 넣고 `console.info`로만 찍는다
- ⚠️ **`NEXT_PUBLIC_*`는 빌드 시점에 코드에 박힌다.** Vercel에서 값을 바꿔도
  **재배포하지 않으면 예전 값이 그대로다**

### 1.3 배포

`main`에 푸시하면 Vercel이 자동 배포한다.
코드 변경 없이 다시 배포: Vercel → Deployments → 최신 → `...` → Redeploy
(Build Cache 체크는 풀어두는 편이 안전하다)

---

## 2. 끝난 것 (P0)

### 2.1 골격

| 항목 | 비고 |
|---|---|
| Next.js 16.3.3 App Router + TS | Vite에서 전환 (`aaafdb1`) |
| **전 페이지 정적 프리렌더** | 서버리스 함수 0. poc-plan 6번의 "서버 0" 원칙, 무료 티어 안에서 끝내려면 이걸 깨면 안 된다 |
| Vercel Hobby | `duckmoim.com` |

### 2.2 화면, `src/components/`

| 파일 | 역할 | 알아둘 것 |
|---|---|---|
| `HomeView.tsx` | 섹션형 홈 (지역 스택 + 지역 버튼) | 오프메이트식 세로 스택. **필터가 없다**, 홈은 "지금 어디서 뭐 하나"만 |
| `ListView.tsx` | 전체 목록 | 여기도 필터 없음 |
| `MapView.tsx` | 지도 | **필터(지역·날짜)는 지도에만 있다.** 가시 반경을 텍스트로 표시 |
| `DateNav.tsx` | 날짜 화살표 네비 | |
| `EventDetail.tsx` | 상세 시트 | 해시 라우팅 `#/e/:id`, 모바일 뒤로가기가 시트를 닫는다 |
| `SearchOverlay.tsx` | 검색 레이어 | 홈을 벗어나지 않는다 (전체 탭으로 튕기면 안 됨) |
| `EventCard.tsx` | 카드 | 이미지 + `onError` 폴백 |
| `Section.tsx` · `Chips.tsx` · `BottomNav.tsx` | | |
| `AnalyticsScripts.tsx` | 계측 스크립트 삽입 | **서버 컴포넌트다.** 2.5의 함정 참조 |

지도는 **기본 마커를 쓰지 않는다.** `CustomOverlay`로 대상명·유형 라벨을 얹는다 
핀만 찍으면 눌러보기 전에 무엇인지 알 수 없다.

### 2.3 로직, `src/lib/`

| 파일 | 내용 |
|---|---|
| `filters.ts` | `DateFilter`, `overlaps()`, `periodLabel()`(D-day 표기), `filterEvents()`, `groupByDistrict()`, `DISTRICT_LABELS` |
| `route.ts` | 해시 라우팅. 라이브러리 없이 `useRoute()` 훅 하나 |
| `kakao.ts` | SDK 로더(`autoload=false`), `distanceKm()` 하버사인 |
| `visual.ts` | 이미지 없을 때 대상명 해시 → 결정적 색블록 |
| `analytics.ts` | `track()`, `trackVisit()`. **`'use client'`** |

`src/types.ts`가 스키마다. poc-plan 5.1을 그대로 옮겼고,
PoC에서 안 쓰는 필드(`trust`, `subject_type`)도 남겼다. 본 서비스 승격 시 매핑 없이 넘기려고.

### 2.4 데이터, **162건** (팝업 13 · 생카 149)

`src/data/events.json`이 앱이 읽는 **유일한** 소스다 (빌드타임 번들).
`events.sample.json`은 개발용 가상 데이터고 앱은 읽지 않는다.

| 지역 | 건수 |
|---|---|
| 홍대 | 97 |
| **etc (미분류)** | **24** ← 5.1 부채 |
| 용산 | 18 |
| 합정 | 8 |
| 성수 · 강남 | 6 · 6 |
| 잠실 · 여의도 | 2 · 1 |

- 이미지 162 / 162 · `trust`: parsed 147 · partner 15 (`official` 0건)
- **`goods` 필드는 162건 전부 빈 배열이다** ← P1의 출발점
- 주말 스파이크 2~3배 (8/29 = 53건, 8/30 = 56건). 금요일 저녁 배포 계획의 근거
- 홍대 97건은 bridge-plan의 "홍대 하루 100곳" 주장과 일치, 데이터로 검증됐다

### 2.5 계측

배포된 HTML에서 **3종 모두 실물 확인했다.**

| 도구 | 상태 | 용도 |
|---|---|---|
| GA4 | 정상 | 커스텀 이벤트 · 재방문 |
| Clarity | 정상 | 세션 리코딩 · 히트맵 (Strict 마스킹) |
| Vercel Analytics | 스크립트 삽입됨 | 방문 수 · 유입 경로. **대시보드 Enable 필요** → 6번 |

구현된 이벤트 4종 (poc-plan 7번 표와 1:1):
`view_home` · `view_detail` · `open_source` · `return_visit`

재방문은 `localStorage`(`moyeora.first_seen` / `moyeora.last_seen`)로 **직접 계상한다.**
GA4 기본 재방문 지표는 쿠키 만료·시크릿창 때문에 과소 계상되고,
같은 날 여러 번 열어도 재방문으로 세지 않는다 (날짜가 바뀔 때만).

> ### ⚠️ 밟았던 함정, 서버 컴포넌트가 클라이언트 상수를 읽으면 안 된다
>
> `lib/analytics.ts`는 `'use client'` 모듈이다. 여기서 export한 상수를
> 서버 컴포넌트가 import하면 **값이 아니라 클라이언트 참조로 치환된다.**
> 실제로 그렇게 짰다가 배포에서 스크립트 URL이 이렇게 나갔다:
>
> ```
> gtag/js?id=function(){throw Error("Attempted to call GA4_ID() from the server...")}
> ```
>
> 게다가 트리가 깨져서 `<Analytics />`까지 렌더되지 않았다.
> **로컬 빌드는 통과하고 Vercel에서만 터졌다.** 커밋 `5a584dd`에서
> `AnalyticsScripts.tsx`가 `process.env`를 직접 읽게 하고
> `analytics.ts`의 상수는 export를 뗐다. 다시 export하지 말 것.

### 2.6 유입 자산

- `src/app/opengraph-image.tsx`, 빌드타임 생성 OG 이미지 (1200×630, 라이브 실측 확인)
- satori는 웹폰트를 스스로 안 받아온다. Google Fonts `text=`로 필요한 글자만 잘라
  받고, **구형 User-Agent를 보내야 woff2 대신 satori가 읽는 TTF가 온다.**
  안 하면 한글이 전부 빈 사각형이 된다
- 빌드 환경에 네트워크가 없을 수도 있어 폰트 실패 시 폰트 없이 렌더하도록 감쌌다

### 2.7 수집 파이프라인, `crawler/`

```bash
node crawler/run.mjs --source all --limit 700 --days 90   # 수집 (느리다, 8분+)
node crawler/to-events.mjs                                # 정규화 → src/data/events.json
```

**필터 기준만 바꿀 때는 `to-events.mjs`만 돌린다.** 수집 원본이
`data/raw/crawl/{popga,offmate}.json`에 남아 있어 다시 긁을 이유가 없다.
상대 서버를 두 번 두드리지 않는다.

| 소스 | 대상 | robots.txt | 추출 |
|---|---|---|---|
| 팝가 | 팝업 | 허용 | 사이트맵 2,830 URL → RSC 페이로드. **좌표 포함 → 지오코딩 불필요** |
| 오프메이트 | 생카 | `Allow: /` | 사이트맵 14,764 URL → `__NEXT_DATA__`. **`artist.groupName` 구조화 → 화이트리스트 불필요** |
| 팝플리 |, | `/api/` 차단 | **우회하지 않는다.** 미사용 |

크롤러 원칙 4개 (CLAUDE.md에도 있음):

1. robots.txt를 **실행 전에** 확인하고, 막혀 있으면 그 소스를 건너뛴다
2. User-Agent에 프로젝트명·저장소 주소를 밝힌다. **브라우저인 척하지 않는다**
3. 요청 간격 **700ms 고정**. 차단 회피가 아니라 부하를 주지 않기 위한 것이라 낮추지 않는다
4. robots.txt가 막은 경로는 건드리지 않는다

**K-pop 판정의 함정**, 한국어는 단어 경계가 없다.
"카이"가 "아카이브"·"서울스카이"에, "조이"가 "조이올팍"에, "리사"가 일본 가수 LiSA에 걸렸다.
→ `LONG_NAME_MIN = 4`: 4자 이상은 부분일치 허용, **4자 미만은 태그 정확일치만.**
누락이 보이면 `crawler/kpop-artists.json`에만 추가하면 된다 (현재 53그룹).

### 2.8 가드레일, 타협하면 안 되는 것

정합성을 안 따진다고 해서 여기를 타협하지 않는다. **되돌릴 수 없는 항목이다.**

- **출처를 속이지 않는다.** `source_url`은 주최자·운영사 실제 원문(인스타·X)만 가리킨다.
  확인하지 않은 것을 `trust: 'official'`로 올리지 않는다 (그래서 official 0건이다).
  리스팅에서 가져온 건 `parsed`다
- **경쟁 리스팅 링크를 화면에 노출하지 않는다.** `listing_url`은 데이터에만 남긴다.
  (한 번 팝가 크레딧을 화면에 박았다가 걷어냈다)
- `data/raw/`는 커밋하지 않는다. 앱에는 **사실 정보(장소·기간·시간)만** 싣고 원문 링크를 반드시 노출한다.
  일정은 저작물이 아니지만 포스터는 저작물이다
- 종료된 이벤트는 목록에서 뺀다. **지난 정보는 없는 정보보다 나쁘다** (poc-plan 4.3).
  필터·정규화 양쪽에서 걸러진다
- 푸터에 게시중단 요청 안내
- 임시 파일(사이트맵 덤프 등)을 **루트에 만들지 않는다.** 스크래치 디렉터리를 쓴다.
  `.gitignore`가 잡고 있지만 8MB를 올렸다가 걷어낸 적이 있다

---

## 3. ★ 계획 변경 제안, 굿즈 재고보다 혼잡도를 먼저

**이 절이 이 문서에서 가장 중요하다.** poc-plan 8·9번의 순서를 뒤집는 제안이다.

### 3.1 확정된 사실, 굿즈 재고 데이터는 존재하지 않는다

팝가에 `spot-offers(GOODS)` 쿼리가 있다. **모든 팝업에서 count = 0이다.**
MD 58종을 파는 빅뱅 팝업도 0이다. 원인은 추측이 아니라 구조다 

> **팝업 굿즈는 현장 판매이고, 온라인 재고 API가 애초에 존재하지 않는다.**

오프메이트 생카 쪽 `specialGoods`는 ID 배열인데, ID→이름 매핑을
`pageProps`와 로드된 JS 청크 6개에서 찾지 못했다. 지금은 "특전 N종"으로만 쓴다.

### 3.2 그래서 F2(재고)를 먼저 하면 무슨 일이 생기나

| 비용 | 내용 |
|---|---|
| 굿즈 마스터 **수동 입력** | 팝업 3~8곳 × 45~120품목. 긁어올 수 없어 전부 손으로 넣는다 |
| 커버리지 | 팝업 13건 = 전체 데이터의 **8%** |
| 런칭 시점 화면 | 마스터를 다 넣어도 **재고 상태는 전부 빈칸.** 제보가 없으니까 |
| 제보 마찰 | 품목을 고른 뒤 상태를 고른다 = **2단계** |

즉 가장 비싼 시드를 넣고도 8%만 채워지고, 그 8%조차 첫날은 빈칸이다.
poc-plan 8번은 **"빈 화면은 훅이 될 수 없다"**고 못 박았는데,
F2를 훅으로 걸면 바로 그 상황이 된다.

### 3.3 혼잡도(F3)를 먼저 하면

| 이점 | 내용 |
|---|---|
| **마스터 데이터가 필요 없다** | 제보 단위가 **장소 자체**다. 이미 162곳 있다 |
| 커버리지 | 팝업 + 생카 **162건 전부** = 100% |
| 제보 마찰 | 4단(`none`/`lt10`/`lt30`/`gt60`) 중 하나 = **탭 1회** |
| 지표 4 측정 가능성 | 마찰이 낮을수록 제보 완료율 표본이 나온다. **P1의 목적은 기능이 아니라 지표다** |
| 재방문 훅 | 대기 시간은 시간 단위로 바뀐다. 정적 리스트에 없던 "돌아올 이유"가 여기서 생긴다 |
| 구현 비용 | 테이블 1개 + RPC 1개 + 시트 1개. F2보다 확실히 싸다 |

**결정적인 건 이거다**, 굿즈는 **팝업에만** 있는데 우리 데이터의 92%는 생카다.
혼잡도는 두 종류 모두에 있다. 데이터가 있는 쪽에 기능을 붙이는 게 맞다.

### 3.4 정직하게 말해야 하는 단점

**혼잡도도 런칭 시점엔 비어 있다.** poc-plan 3.1이 이미 지적했다 
생카 주최자는 대기를 공지하지 않으므로 **생카 대기 시드는 0건**이고,
팝업 웨이팅 마감 공지만 일부 있다.

그래서 poc-plan 8번은 "빈 화면을 훅으로 걸면 첫 화면에서 이탈한다"며 F3를 훅에서 뺐다.
**이 지적은 여전히 유효하다.** 다만 F2도 3.2에서 똑같이 비게 된다는 게 새로 밝혀진 사실이고,
그러면 판단 기준이 "어느 쪽이 안 비나"에서 **"어느 쪽 빈칸이 더 싸고 더 빨리 채워지나"**로 바뀐다.

- 빈칸을 만드는 비용: F2는 마스터 입력 몇 시간, F3는 0
- 빈칸이 채워지는 속도: F2는 품목 단위 제보(느림), F3는 장소 단위 탭 1회(빠름)
- 채워졌을 때 덮는 범위: F2는 8%, F3는 100%

### 3.5 그럼 홍보 훅은 무엇으로 하나

원래 훅 **"팝업 굿즈 뭐가 품절됐는지, 공식 공지 모아서 한 화면에"**는
3.1 때문에 근거를 잃었다. poc-plan 11번 리스크 표의 완화책
*"공식 품절 공지 15건 미만이면 훅을 축소"* 가 발동 조건에 들어와 있다.

**세 가지 안. 위에서부터 추천이다.**

**(A) 위치 축 + 실시간 참여** ← 추천
> "오늘 홍대에서 열리는 생카 97곳, 지도 한 장에.
> 어디가 붐비는지는 다녀온 사람들이 알려줘요."

- 지금 **실제로 가진 것**(162건 · 홍대 97)으로 훅을 만든다. 첫 화면이 안 빈다
- 혼잡도는 "우리가 제공하는 정보"가 아니라 **"같이 만드는 정보"**로 포지셔닝한다.
  빈칸이 결함이 아니라 참여 지점이 된다 (poc-plan 3.2 프레이밍)
- 제보를 부가 메시지로 얹어 지표 4를 연다

**(B) 공식 라인업 모아보기**, poc-plan 11번이 지정한 폴백
> "흩어진 공식 공지, 한 곳에서."

- 안전하지만 약하다. 리스팅과 구분이 잘 안 된다

**(C) 원안 유지 (굿즈 품절)**
> 마스터 입력을 감수하고 팝업 13건에 집중

- 굿즈 재고가 진짜 킬러라고 판단되면 이걸 택한다.
  다만 **8% 커버리지 · 첫날 전부 빈칸**을 안고 간다는 걸 알고 결정해야 한다

> **판단 기준**, PoC의 산출물은 화면이 아니라 숫자다.
> 지표 0(방문 300)이 1차 관문이고, 통과하지 못하면 뒤 지표는 해석 자체가 무의미하다.
> 유입을 최대로 만드는 훅을 골라야 하고, 그건 **첫 화면이 비지 않는 쪽**이다.

---

## 4. 남은 작업

### P1, 현장 상황 (최우선)

**F2·F3를 팝업 상세 한 화면에 묶는다** (poc-plan 9번).
신선도 표시·`trust` 배지·제보 시트·감쇠 로직이 두 기능 공통이라
따로 만들면 같은 걸 두 번 만든다.

**단 3번의 제안대로 순서는 4.2(혼잡도) → 4.3(재고)로 뒤집는다.**
4.1 인프라와 4.4 공통 UI는 순서와 무관하게 먼저 필요하다.

#### 4.1 인프라 (공통, 선행)

- [ ] Supabase 프로젝트 생성 (Free tier)
- [ ] 테이블 2개 `crowd_report` · `stock_report`, **poc-plan 5.2의 SQL을 그대로 쓴다**
- [ ] RPC 2개 `report_crowd` · `report_stock`
      - 반경 판정은 **PostGIS 없이 하버사인**으로 계산한다.
        장소 좌표를 인자로 받으면 **DB에 장소 테이블을 두지 않아도 된다**, 이게 설계 의도다
- [ ] 어뷰징 방어 트리거, `reporter_id` 기준 **동일 장소 10분 내 재제보 금지**
      - 서버가 없어 IP 레이트리밋을 못 건다. PoC 표본에선 이 정도로 충분하다
- [ ] `reporter_id`는 localStorage UUID
- [ ] `NEXT_PUBLIC_SUPABASE_URL` / `_ANON_KEY` → `.env.local` + Vercel + **재배포**
- [ ] RLS 확인, anon 키가 나가므로 insert는 RPC(`security definer`)로만,
      직접 테이블 쓰기는 막는다

#### 4.2 팝업 혼잡도 (F3) ← **먼저**

- [ ] 대기 4단 `none` / `lt10` / `lt30` / `gt60`
- [ ] 제보 = **탭 1회**로 끝내기. 확인 다이얼로그 넣지 말 것 (마찰이 지표를 죽인다)
- [ ] 지도 핀 색상에 혼잡도 반영 (`MapView.tsx`의 `CustomOverlay`)
- [ ] 공식 웨이팅 마감 공지 시드 수집, **poc-plan 3.1 규칙 엄수**
      - 넣는다: "금일 웨이팅 마감" (팝업 공식)
      - **안 넣는다**: "웨이팅 30분쯤 되는 듯" (일반 트윗), 우리가 지어낸 값
- [ ] **GPS 게이트는 끄되 판정은 기록한다** (poc-plan 3.3)
      - 게이트를 켜면 커뮤니티 유입 방문자 대부분이 버튼을 못 눌러
        **지표 4 측정이 불가능**해진다
      - 제보는 누구나 가능 → RPC가 200m 판정만 해서 `in_range` 플래그 저장
      - 화면에서는 현장 제보에만 `현장 확인` 배지
      - 게이트 자체는 본 서비스에서 되살린다

#### 4.3 굿즈 재고 현황 (F2) ← 그다음

- [ ] 굿즈 마스터 입력, 팝업 3~8곳 × 45~120품목. **긁어올 수 없다 (3.1)**
- [ ] `EventItem.goods` 채우기 (현재 162건 전부 빈 배열)
      - `Goods.is_random`을 활용할 것, 랜덤 품목은 "품절" 개념이 아니라
        "지금 뭐가 나오나"가 관심사다
- [ ] 재고 3단 `in_stock` / `sold_out` / `limited`
- [ ] **품절 표기는 회색 + 취소선.** 빨강 금지, 카드에서 accent(핑크)와 부딪힌다
- [ ] 공식 품절 공지 시드, `trust: 'parsed'` + `source_url` 필수

#### 4.4 공통 UI (양쪽에 걸침)

- [ ] 제보 시트, 재고·대기 **한 컴포넌트**로
- [ ] **신선도 감쇠** (poc-plan 4.3), **크론 없이 읽기 시점에 계산**

  | 경과 | 표시 |
  |---|---|
  | ~10분 | 진하게 + `방금` |
  | 10~30분 | 보통 + `N분 전` |
  | 30분~ | **흐리게** + `N분 전` |
  | 24시간~ | 미표시 |

- [ ] `trust` 배지, 공식 / 정보 원문 / 제보 구분
- [ ] 빈 상태를 참여 유도로 (poc-plan 3.2)
      "정보 없음" ❌ → **"아직 제보가 없어요 · 첫 제보 남기기"** ✅
- [ ] 계측 2종 `report_open` · `report_submit` (`kind`로 재고/대기 구분)
      - `EventName` 유니온에 추가 전 **poc-plan 7번 표를 먼저 고친다.**
        재지 않을 것을 쏘면 노이즈만 는다

> ### ⚠️ 시드 시각 처리, 여기가 F3 가치의 핵심이다
>
> `reported_at`에 **원문 게시 시각**을 넣는다. **`now()`로 찍지 않는다.**
>
> `now()`를 넣는 순간 수집이 아니라 **조작**이 되고, F3의 가치인 신선도 자체가 무너진다.
> 원문 시각을 넣으면 감쇠(4.3)에 자연히 걸려 "2시간 전 정보"로 흐리게 표시된다.
> 즉 시드는 "지금 재고 있음"을 주장하지 않고 **"N시간 전엔 이랬다"**를 보여준다.
> 정확하면서 화면도 채운다.

### P2, 담기 / 내 코스 (2h)

- [ ] localStorage 기반 담기 · 내 코스 화면 · 계측 `save_course`

> **P2가 P3보다 위인 이유**, 생카 F3는 런칭 시 전부 빈칸이라 즉시 지표를 못 만든다.
> 담기는 데이터가 이미 162건 있어 첫날부터 지표 3이 찍힌다.

### P3, F3를 생카 전체로 확장 (1.5h)

- [ ] 149건에 핀 색상 + 제보 그릇
      (3번 제안을 채택하면 4.2에서 이미 일부 흡수된다)

### P4, 여유가 나면 (3h)

- [ ] Supabase Realtime 구독, 컷되면 **진입 시 1회 조회로 충분하다**
- [ ] 계측 `filter_change`
- [ ] PWA manifest

---

## 5. 데이터 품질 부채

### 5.1 `etc` 미분류 24건, 조치법까지 확인해뒀다

24건 중 **22건이 마포구**다. 즉 실제로는 홍대·합정인데 `DISTRICT_RULES`의
도로명 패턴에 안 걸린 것뿐이다. 지역 축 제품에서 두 번째로 큰 버킷이라 눈에 띈다.

| 누락된 도로명 | 실제 지역 | 예시 |
|---|---|---|
| 신촌로 / 신촌로2안길 / 신촌로4길 | 홍대 | 카페티노, CAFE07, 더 누크 |
| 동교로34길 / 동교로30길 | 홍대 | 해피벌스데이, 스튜디오앱트 |
| 성미산로26길 | 합정 | 페이버릿피스 |
| 월드컵로11길 / 월드컵북로6길 | 합정 | 스캐터북스, 리드미 |
| 희우정로1길 | 합정 | 알토커피바 합정 |

나머지 2건은 성동구 왕십리로(→ 성수 확장 검토), 강동구 성안로(→ `etc` 유지가 맞다).

**조치**, `crawler/to-events.mjs`의 `DISTRICT_RULES`에 패턴 추가 후
**`to-events.mjs`만 재실행**한다. 다시 긁을 필요 없다.

### 5.2 나머지

| 항목 | 규모 | 상태 |
|---|---|---|
| `specialGoods` ID→이름 매핑 | 전 생카 | `pageProps`·JS 청크 6개에서 미발견. "특전 N종" 표기 유지 |
| `reservation_url` | 3 / 162 | 원문에 없는 경우가 대부분. 정상 |
| `trust: 'official'` | 0건 | **의도된 것.** 확인한 것만 official로 올린다 |

---

## 6. 오너 쪽 남은 조작

- [ ] **Vercel 프로젝트 → Analytics 탭 → Enable** (재배포 불필요, 즉시 적용)
      스크립트는 붙어 있는데 활성화하지 않으면 데이터가 안 쌓인다
- [ ] GA4 스트림 URL → `https://duckmoim.com`
      (원치 않는 리퍼러 나열에 실제 값이 있다)
- [ ] Clarity 웹사이트 URL 동일하게 (표기용)
- [x] 카카오 개발자센터에 배포 도메인 등록

---

## 7. 관측 계획 (poc-plan 1번)

배포 후 8/29 ~ 9/5.

| 지표 | 목표 | 원천 | 선행 작업 |
|---|---|---|---|
| 0 · 방문 | 300 | Vercel Analytics | 완료 |
| 1 · 체류 | 90초 | GA4 | 완료 |
| 2 · 상세 진입률 | 40% | `view_detail` / `view_home` | 완료 |
| 3 · 담기 발생률 | 15% | `save_course` | **P2** |
| 4 · 제보 완료율 | 3% | `report_submit` | **P1** |
| 5 · 7일 재방문률 | 20% | `return_visit` | 계측 완료 / 훅은 **P1** |

**지표 0이 1차 관문이다.** 표본이 작으면 재방문률은 통계적으로 무의미하므로,
0을 통과했을 때만 뒤 지표를 해석한다.

**지표 3·4는 P1·P2를 만들지 않으면 측정 자체가 불가능하다.**
지금 배포된 상태로 관측을 시작하면 6개 중 3개만 나온다.

### 유입 (poc-plan 8번), 여기가 진짜 병목

만들어놓고 기다리면 방문자는 0에 수렴한다.

| 경로 | 기대 | 비고 |
|---|---|---|
| 커뮤니티 1회 포스팅 | 수백~수천 | 링크 하나로 규모가 나오는 **유일한** 채널 |
| X 검색 유입 | 수십 | 수는 적지만 의도가 명확 |
| OG 이미지 |, | 없으면 클릭률 반토막. **완료** |

채널별로 **UTM을 분리**해서 쏜다. 채널별 체류·재방문 차이가 곧 타깃 정보다.

---

## 8. 코드 관습 (CLAUDE.md 요약)

- **날짜는 `'YYYY-MM-DD'` 문자열.** `Date` 객체로 왕복하지 않는다.
  이 포맷은 사전순 비교가 곧 날짜 비교라 타임존 버그의 원천이 사라진다
- **오늘 날짜는 `useEffect`에서 확정한다.** 서버 프리렌더 시점은 빌드 시각이라
  그대로 쓰면 배포 다음날부터 하이드레이션이 어긋난다
- 커스텀 이벤트는 **GA4에만** 쏜다. Vercel Analytics는 방문 수·유입 경로 전용 (Hobby 한도)
- 계측 스크립트는 `afterInteractive`, 첫 렌더를 늦추면 그 자체가 이탈을 만들어
  재려던 지표를 왜곡한다
- **라이트 전용.** 커뮤니티 스크린샷·OG와 실제 화면의 톤을 하나로 통제한다
- 좌우 시작선은 `--gutter` 하나로 관리한다. 개별 `16px`을 쓰지 않는다
  (아이템 3개 vs 4개 이상에서 정렬이 깨진 적 있다. 원인은
  `scroll-snap-type: x proximity`가 스크롤 가능할 때만 작동해서였다. snap을 제거했다)
- 핑크 배경 위 텍스트는 `--accent-ink` (`--accent`는 4.5:1 미달)
- 입력 필드 `font-size: 16px` 미만 금지 (iOS Safari 확대)

### 커밋

단계가 끝나면 **커밋과 푸시를 같이** 한다.
커밋 메시지에는 무엇을 했는지보다 **왜 그렇게 했는지**를 남긴다 
다음에 같은 결정을 다시 논쟁하지 않기 위해서다.

```
5a584dd fix: 서버 컴포넌트가 클라이언트 전용 상수를 읽던 문제
f801f57 feat: OG 이미지 + 공유 메타데이터
26ed3c2 feat: 오프메이트 생카 수집 + 계측 3종 + CLAUDE.md
045c999 feat: 팝가 크롤러 + 실 데이터 + 지도 + 날짜 네비게이터
```

---

## 9. 다음 사람이 처음 할 일 (순서)

1. `poc-plan.md`를 읽는다. 특히 **1번(판정 기준) · 3.1(시드 규칙) · 9번(우선순위)**
2. **3번을 읽고 훅을 결정한다.** 이게 안 정해지면 P1을 어느 쪽부터 할지 못 정한다
   → **오너 결정 필요 사항이다**
3. `npm install && npm run dev`로 화면을 본다. 162건이 뜨는지 확인
4. 5.1 `etc` 24건 정리, **30분이면 되고 즉시 눈에 보이는 개선이다.**
   워밍업으로 적당하다
5. 4.1 Supabase 인프라 → 결정된 순서대로 4.2 / 4.3
